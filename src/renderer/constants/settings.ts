export enum Blockchain {
  Ethereum = 'Ethereum',
}

export enum EmissionMode {
  DefaultBatching = 'default batching',
  SafeBatching = 'safe batching',
  Streaming = 'streaming',
}

export enum ConnectionMode {
  WebSocket = 'WebSocket',
  IPCSocket = 'IPC Socket',
}
